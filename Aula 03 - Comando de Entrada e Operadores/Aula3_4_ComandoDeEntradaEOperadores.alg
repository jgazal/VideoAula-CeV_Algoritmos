algoritmo "semnome"
var
   //A:Inteiro
   A:Real
inicio
      //A <- Abs(-50)
      //A <- Exp(9,5)
      //A <- RaizQ(81)
      //A <- Int(RaizQ(90))
      //A <- GraupRad(90)
      A <- Sen(GraupRad(90))
      Escreva(A)
fimalgoritmo
